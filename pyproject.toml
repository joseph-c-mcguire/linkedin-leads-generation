[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "linkedin-leads-generation"
version = "0.1.0"
dependencies = [
    "requests>=2.31.0"
]
requires-python = "==3.11.9"
